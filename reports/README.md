# Generated reports

- `sample-alerts.json` contains the full structured report, configuration, analyst
  actions, and matching raw evidence.
- `sample-alerts.csv` contains one summary row per alert for spreadsheet or SIEM
  ingestion testing.

Regenerate both files with the commands in
[`docs/VALIDATION.md`](../docs/VALIDATION.md).
